[build]
builder = "nixpacks"
nixpacksConfigPath = "backend/nixpacks.toml"

[deploy]
startCommand = "cd backend && node app.js"
healthcheckPath = "/api/v1/leagues"
restartPolicyType = "on_failure"
restartPolicyMaxRetries = 3
